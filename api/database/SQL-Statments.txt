Select occupancydate,
 start_time, 
 end_time, 
 location, 
 area, 
 roomnumber, 
 status,
  first_name,
  last_name
 from occupancy
  INNER JOIN Rooms on occupancy."ID rooms" = rooms.ID
  INNER JOIN Status on occupancy."ID status" = status.ID
  inner join User on occupancy."ID user" = user.ID
  where username = X order by Occupancy.occupancydate ;
  
Delete from Occupancy where ID = x;

UPDATE Occupancy
SET Occupancydate = 'X',
 start_time = 'X', 
  end_time = 'X', 
  location = 'X', 
  area = 'X', 
  roomnumber = 'X' 
WHERE  occupancydate = 'Y'and
 start_time = 'Y'and 
 end_time = 'Y'and 
 location = 'Y'and 
 area = 'Y'and 
 roomnumber = 'Y';
 
Select username,
password from Login
 where username = 'Usereingabe 1'
  and password = 'Usereingabe 2';
  
Select rooms, description from rooms; 
Select location from rooms; 
Select area from rooms; 

Delete from rooms where ID = x and Location = y and area = x;
INSERT INTO rooms ( ID, location, area )
Values('x','y','z');

Insert into user ( role, first_name, last_name)
Values('x','y','z');
Insert INTO Login ( username, passwort)
Values('x','y');
Delete from user where ID = x and first_name = y and last_name = v and area = z;


Select  location , 
  area,
   number 
   where location = 'X'and 
  area = 'X'and 
  roomnumber = 'X' ;
  
Select 
   occupancydate,
start_time, 
 end_time, 
first_name, 
last_name,
status,
slot
from occupancy
inner join User on occupancy."ID user" = user.ID
INNER JOIN Status on occupancy."ID status" = status.ID
Inner join Slot on status.ID = slot."ID status"
where occupancydate = 'Aktuelles Datum'
 




